<p style="color: green"><%= notice %></p>

<h1>Notes</h1>


<h4> <%= link_to "New note", new_wiki_post_path %></h4>
<h4><%= link_to "View Deleted Posts", deleted_wiki_posts_path %></h4>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Created at</th>
      <th>Updated at</th>
      <th>Author at</th>

    </tr>
  </thead>
  <tbody>
    <% @wiki_posts.each do |post| %>
      <tr>
        <td><%= link_to post.title, wiki_post_path(post) %></td>
        <td><%= post.created_at.strftime('%B %d, %Y') %></td>
        <td><%= post.updated_at.strftime('%B %d, %Y') %></td>
        <td><%= post.author&.titleize %></td>

      </tr>
    <% end %>
  </tbody>
</table>

<h2>Site contributors: </h2>
<ul>
  <% @wiki_posts.contributors.compact.each do |contributor| %>
    <li><%= contributor.titleize %></li>
  <% end %>
</ul>



